import { AddIncomingTrustee } from './add-incoming-trustee';

describe('AddIncomingTrustee', () => {
  it('should create an instance', () => {
    expect(new AddIncomingTrustee()).toBeTruthy();
  });
});
